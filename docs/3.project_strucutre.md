# Project Structure (Detailed)

## Project Structure – SearchFlow

This document explains the complete folder and file structure of the project.

---

## 📁 Root Directory

searchflow/
│
├── app/ # Application source code
├── docs/ # Documentation files
├── tests/ # Test suite
├── docker/ # Docker-related files
├── docker-compose.yml # Multi-service setup
├── requirements.txt # Python dependencies
└── README.md # Project overview

---

## 📁 app/ (Core Application)

app/
│
├── main.py # FastAPI entry point
├── api/ # API routes
│ └── search.py # /search endpoint
│
├── mcp/ # MCP server & tools
│ └── search_tool.py # Search tool interface
│
├── search/ # Search integrations
│ └── searxng_client.py # SearXNG HTTP client
│
├── ai/ # AI & reasoning logic
│ └── dspy_pipeline.py # DSPy reasoning pipeline
│
├── schemas/ # Data models
│ └── search.py # Pydantic schemas
│
├── core/ # Core utilities
│ ├── config.py # Environment variables
│ └── logging.py # Logging setup
│
└── utils/ # Helper functions
└── text.py # Text cleanup helpers

---

## 📁 docs/ (Documentation)

docs/
│
├── TECH_STACK.md # Technology stack
├── V1_SETUP.md # Version 1 setup guide
└── ARCHITECTURE.md # (optional) deep architecture notes

---

## 📁 tests/ (Testing)

tests/
│
├── test_health.py # API health tests
├── test_search_api.py # /search endpoint tests
└── test_e2e.py # End-to-end tests

---

## 📁 docker/ (Containerization)

docker/
│
├── Dockerfile.api # FastAPI container
└── Dockerfile.mcp # MCP server container

---

## 📄 Other Files

- `docker-compose.yml` – Runs API + SearXNG together
- `requirements.txt` – Python dependencies
- `README.md` – High-level project overview

---

3️⃣ V1_SETUP.md (Version 1 Setup Guide)

### SearchFlow – Version 1 Setup Guide

This document explains how to set up and run **SearchFlow v1**, which focuses on:

- Natural language search
- Live web search
- AI cleanup
- JSON + Markdown output

---

## ✅ Prerequisites

Make sure you have:

- Python 3.10+
- Docker & Docker Compose
- Git

---

## 📥 Clone the Repository

```bash
git clone https://github.com/your-username/searchflow.git
cd searchflow

📦 Install Dependencies (Local)
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

🐳 Run with Docker (Recommended)
docker-compose up --build
This starts:

FastAPI server

SearXNG search engine

🚀 Run the API

Once running, the API is available at:

http://localhost:8000
Swagger UI:

http://localhost:8000/docs

🔍 Example Search Request
curl -X POST http://localhost:8000/search \
  -H "Content-Type: application/json" \
  -d '{"query": "Best FastAPI practices"}'

📤 Example Response
JSON Output
{
  "query": "Best FastAPI practices",
  "results": [...],
  "confidence": 0.87
}

Markdown Output
## Best FastAPI Practices

- Use dependency injection
- Prefer async endpoints
- Validate inputs with schemas

🧪 Run Tests
pytest

🎯 What v1 Includes

✅ Natural language queries
✅ Live web search
✅ AI cleanup & structuring
✅ JSON + Markdown output
✅ Dockerized setup

🚧 What v1 Does NOT Include (Yet)

Authentication

Caching

Vector database

UI dashboard

User accounts

These are planned for future versions.

🏁 Version 1 Goal

Deliver a working, reliable backend that converts live web search results into structured, trustworthy knowledge.
***

## ✅ What You Have Now

You now have:
- A **clear tech stack document**
- A **professional project structure**
- A **clean v1 setup guide**
- A repo that looks **serious and production-ready**

***

### Next things I can do for you:
- Create **V2 roadmap**
- Write **API reference docs**
- Convert this into **GitHub Issues**
- Add **example responses**
- Help you design **auth, caching, or RAG**

Just tell me what’s next 👌
```
